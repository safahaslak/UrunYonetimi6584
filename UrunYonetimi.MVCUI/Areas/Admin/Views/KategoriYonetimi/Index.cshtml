@model IEnumerable<UrunYonetim6584.Entities.Category>

@{
    ViewBag.Title = "Kategoriler";
}


@*Bu Index i eklerken ilk önce Template olarak List i seçtik ve daha sonrasında hangi Model Class seçmemiz gerektiğini seçtik,
bu da Entities den Category olmalı ki database den gelecek olan verileri listeleyelim fakat model class içinde yoktu çünkü reference 
kısmında ekli değildi, add reference kısmında BL ve Entities katmanlarını ekledikten sonra model class da Entities seçilip Index i oluşturduk.*@


<h2>Index</h2>

<p>
    @Html.ActionLink("Yeni Ekle", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreateDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
